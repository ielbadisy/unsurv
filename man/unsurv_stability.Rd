% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summaries.R
\name{unsurv_stability}
\alias{unsurv_stability}
\title{Stability assessment for an unsurv clustering}
\usage{
unsurv_stability(
  S,
  times,
  fit,
  B = 30,
  frac = 0.5,
  mode = c("bootstrap", "subsample"),
  jitter_sd = 0.01,
  weight_perturb = 0.3,
  eps_jitter = 0.02,
  return_distribution = TRUE
)
}
\arguments{
\item{S}{Numeric matrix of survival probabilities used for stability assessment
(\eqn{n \times m}), with columns matching \code{times}.}

\item{times}{Numeric vector of time grid points (length \eqn{m}).}

\item{fit}{An object of class \code{"unsurv"}, typically returned by \code{\link{unsurv}}.}

\item{B}{Integer; number of resamples.}

\item{frac}{Numeric in (0, 1]; fraction of rows sampled per resample.}

\item{mode}{Resampling mode: \code{"bootstrap"} (with replacement) or
\code{"subsample"} (without replacement).}

\item{jitter_sd}{Nonnegative numeric; curve-space noise level applied before clamping/monotone enforcement.}

\item{weight_perturb}{Numeric in \eqn{[0,1]}; blends trapezoidal weights with a random simplex
to perturb the weighting scheme.}

\item{eps_jitter}{Nonnegative numeric; feature-space jitter used inside the clustering during resamples.}

\item{return_distribution}{Logical; if \code{TRUE}, returns the full ARI distribution, else returns only the mean.}
}
\value{
If \code{return_distribution = TRUE}, a list with:
\itemize{
\item \code{mean}: mean ARI across resample-pair overlaps
\item \code{aris}: numeric vector of ARIs
}
Otherwise, returns a single numeric mean ARI.
}
\description{
Computes a resampling-based stability score for a fitted \code{unsurv} model
using the Adjusted Rand Index (ARI) computed on overlap sets across resamples.
}
\examples{
if (requireNamespace("cluster", quietly = TRUE)) {
  set.seed(2025)
  n <- 120; Q <- 60
  times <- seq(0, 5, length.out = Q)
  rates <- c(0.12, 0.38, 0.8)
  grp <- sample(1:3, n, TRUE, c(0.4, 0.4, 0.2))
  S <- t(vapply(1:n, function(i)
    pmin(pmax(exp(-rates[grp[i]] * times) + rnorm(Q, 0, 0.01), 0), 1),
    numeric(Q)
  ))

  fit <- unsurv(S, times, K = NULL, K_max = 6, distance = "L2",
               enforce_monotone = TRUE, standardize_cols = FALSE,
               eps_jitter = 0, seed = NULL)

  stab <- unsurv_stability(S, times, fit, B = 30, frac = 0.55, mode = "bootstrap",
                           jitter_sd = 0.3, weight_perturb = 0.0, eps_jitter = 0.3,
                           return_distribution = TRUE)
  stab$mean
}
}
